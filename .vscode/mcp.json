{
	"servers": {
		"tavily_search": {
			"type": "stdio",
			"command": "bash",
			"args": [
				"-lc",
				"set -a && source .env && set +a && exec npx -y mcp-remote \"https://mcp.tavily.com/mcp/?tavilyApiKey=$TAVILY_API_KEY\""
			]
		}
	},
	"inputs": []
}
